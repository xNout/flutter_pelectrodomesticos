import 'package:flutter/material.dart';

class Contactanos extends StatelessWidget {
  const Contactanos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Contáctanos'),
      ),
      body: Center(
        child: Text('Contenido de Contáctanos'),
      ),
    );
  }
}
